# Compatibility placeholder retained for upgrades from earlier Odin 3 audio
# builds. Stereo downmixing now happens entirely inside the Stereo filter graph
# so this fragment cannot alter Bluetooth, headphone, or other external audio.

stream.properties = { }
